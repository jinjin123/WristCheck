(function ($, Drupal) {
    $(function () {
        // 这里没法这么写，每次选择字母后UI都会刷新
        // var brands_name_list = $('.wc-all-brands-header')
        // var list_item = brands_name_list.find('.wc-all-brands-header-list>.wc-all-brands-header-list__href');

        // if (list_item.length > 2) {
        //     var A = $(list_item[0]).text().trim();
        //     var Z = $(list_item[list_item.length - 1]).text().trim();
        //     console.log(A, Z);
        //     brands_name_list.find('.wc-all-brands-header-name').text(A + '-' + Z)
        // }
    })
})(jQuery, Drupal);